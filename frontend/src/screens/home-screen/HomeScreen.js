import React from 'react';
 
const HomeScreen = () =>  {
  return (
    <div>
      <h1>HomeScreen</h1>
    </div>
  );
}

/**
 * const result = await axios.post ("nodehost:nodeport", {
 * id: id,
 * name: name,
 * })
 */

export default HomeScreen;
